[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npx vite"
  port = 8888
  targetPort = 5173

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"


[build.environment]
  NODE_VERSION = "18"
